<UserControl x:Class="LibraryManagementFE.Controls.MetricCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        Reusable metric / KPI card.
        Properties: IconBackground, IconForeground, BadgeBackground, BadgeText,
                    BadgeForeground, Value, Label
    -->
    <Border Style="{StaticResource Card}" Padding="24" Width="220">
        <StackPanel>

            <!-- Row: icon + badge -->
            <DockPanel LastChildFill="False" Margin="0,0,0,16">
                <!-- Icon container -->
                <Border DockPanel.Dock="Left"
                        Background="{Binding IconBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CornerRadius="8" Padding="8" VerticalAlignment="Center">
                    <ContentPresenter Content="{Binding IconContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      Width="16" Height="16"/>
                </Border>

                <!-- Badge pill -->
                <Border DockPanel.Dock="Right"
                        Background="{Binding BadgeBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CornerRadius="9999" Padding="8,4" VerticalAlignment="Center">
                    <TextBlock Text="{Binding BadgeText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Foreground="{Binding BadgeForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               FontFamily="{StaticResource InterFont}"
                               FontSize="12"/>
                </Border>
            </DockPanel>

            <!-- Value -->
            <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontFamily="{StaticResource InterFont}"
                       FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource TextHeadingBrush}"
                       Margin="0,0,0,4"/>

            <!-- Label -->
            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontFamily="{StaticResource InterFont}"
                       FontSize="12" FontWeight="Medium"
                       Foreground="{StaticResource TextMutedBrush}"/>
        </StackPanel>
    </Border>
</UserControl>
