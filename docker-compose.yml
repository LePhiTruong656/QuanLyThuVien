services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: quanlythuvien-sql
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Library_Dev_Pass123!"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql

volumes:
  sqlserver_data:
